package com.aws.backend.repo;

import com.aws.backend.domain.Prerequi;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PrerequiRepo extends JpaRepository<Prerequi, Integer> {
}
